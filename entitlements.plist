<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- PyInstaller extracts .so/.dylib to temp dir at runtime; they aren't individually signed -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Python dynamic module loading needs JIT -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Extracted libs and bundled FFmpeg aren't signed with same team identity -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- PyInstaller may set DYLD paths -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Microphone access for medical transcription -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Network access for AI/STT provider API calls -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- File access for database, recordings, and exports -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
